jQuery(document).ready(function ($) {
	// This prep function does two things
	// 1. Loads all of your javascript once the document is ready
	// 2. Recreates the "$" alias for use because WP automatically loads jQuery in noConflict() mode
});
